# Copy this file to .env and add real credentials. Never commit .env.
WEATHER_API_KEY=
GROQ_API_KEY=
# A current Groq chat model. Change it if your account uses a different model.
GROQ_MODEL=llama-3.3-70b-versatile
PORT=3000
